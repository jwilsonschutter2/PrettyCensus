# PrettyCensusV2
Repository-base rebuild with Compare to Another Year removed, multi-column preset bundles, a centralized preset library, friendly HTML/CSV headers, and friendly GeoJSON map-export metadata.

Edit `js/config/preset-library.js` to add variables or presets. A preset uses `columns: ["VARIABLE_ID", ...]` and may include any number of columns.
